<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Spotui" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <style name="splashScreenTheme" parent="Theme.SplashScreen">
        <!-- Same artwork as the launcher icon, so the splash flash matches the launcher icon -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Spotui</item>
    </style>
</resources>

